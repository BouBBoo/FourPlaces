<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TD2.MainPage">

    <StackLayout Margin="5,5,5,5" VerticalOptions="Center">
        <!-- Place new controls here -->
        <Label Text="Login:"/>
        <Entry Text="{Binding LOGIN}" x:Name="Login" Placeholder="example@mail.com"></Entry>
        <Label Text="Password :"/>
        <Entry Text="{Binding PASSWORD}" x:Name="Password" IsPassword="true" Placeholder="password"></Entry>
        <Button Text="Login" Command="{Binding goToLogin}" HorizontalOptions="Fill"/>
        <Button Text="Create Account" Command="{Binding goToCreation}"/>

    </StackLayout>

</ContentPage>
