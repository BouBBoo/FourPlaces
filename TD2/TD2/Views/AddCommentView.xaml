<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TD2.Views.AddCommentView">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Commentaire" FontSize="Large" HorizontalOptions="Center" VerticalOptions="Center"/>
            <Entry Text="{Binding CommentText}" HeightRequest="500" HorizontalOptions="FillAndExpand"/>
            <Button Text="Valider" Command="{Binding submitComment}" HorizontalOptions="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>